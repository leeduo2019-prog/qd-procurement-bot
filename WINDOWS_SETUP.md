# Windows 10 定时任务设置指南

## 📦 第一步：准备文件

### 需要复制到另一台电脑的文件

将整个项目文件夹复制到另一台 Windows 10 电脑：

```
qd-procurement-crawler/
├── main.py                 # 主程序
├── crawler.py              # 爬虫模块
├── dingtalk_notifier.py    # 钉钉推送模块
├── .env                    # ⚠️ 钉钉配置（必须复制！）
├── requirements.txt        # Python 依赖
├── setup_windows_task.bat  # 定时任务设置脚本
└── QUICKSTART.md           # 快速入门文档
```

**⚠️ 重要提示：**
- `.env` 文件包含您的钉钉 Webhook 和加签密钥，**必须复制**
- 如果忘记复制，需要重新创建并填写配置

---

## 🛠️ 第二步：安装 Python

### 2.1 下载 Python

1. 打开浏览器，访问：https://www.python.org/downloads/
2. 下载 **Python 3.10+** 版本（推荐 Python 3.11）
3. 运行安装程序

### 2.2 安装时的重要设置

**安装界面勾选以下选项：**
- ✅ **Add Python to PATH**（必须勾选！）
- ✅ Install for all users（可选）

然后点击 **Install Now**

### 2.3 验证安装

打开 **命令提示符**（Win + R → 输入 `cmd` → 回车），输入：

```cmd
python --version
```

如果显示 `Python 3.x.x`，说明安装成功！

---

## 📥 第三步：安装项目依赖

### 3.1 打开命令提示符

按 `Win + R` → 输入 `cmd` → 回车

### 3.2 进入项目目录

```cmd
cd /d D:\qd-procurement-crawler
```

> 将路径替换为您实际存放项目的目录

### 3.3 安装依赖包

```cmd
pip install -r requirements.txt
```

等待安装完成，看到 `Successfully installed ...` 表示成功。

---

## ✅ 第四步：测试运行

### 4.1 测试爬虫运行

```cmd
python main.py
```

**预期结果：**
- 自动打开浏览器（无头模式）爬取数据
- 显示匹配的公告数量
- 钉钉群收到推送消息

---

## ⏰ 第五步：设置定时任务

### 5.1 以管理员身份运行设置脚本

**方法 1：右键菜单**
1. 找到 `setup_windows_task.bat` 文件
2. **右键点击** → 选择 **以管理员身份运行**

**方法 2：命令提示符**
```cmd
# 以管理员身份打开 cmd，然后运行：
setup_windows_task.bat
```

### 5.2 确认任务创建

看到以下提示表示成功：

```
============================================================
[成功] 定时任务创建完成！
============================================================

任务名称：青岛政府采购爬虫
运行时间：每天早上 9:00
项目路径：D:\qd-procurement-crawler
```

---

## 🔍 第六步：验证定时任务

### 6.1 打开任务计划程序

1. 按 `Win + R`
2. 输入 `taskschd.msc`
3. 回车

### 6.2 查找任务

在左侧树形菜单中：
1. 点击 **任务计划程序库**
2. 在右侧列表中找到 **青岛政府采购爬虫**

### 6.3 查看任务详情

双击任务，查看：
- **常规** 标签：确认已启用
- **触发器** 标签：每天 9:00
- **操作** 标签：启动程序 `python.exe`，参数 `main.py`

---

## 🧪 第七步：手动测试定时任务

### 立即运行一次

**方法 1：任务计划程序**
1. 右键点击 **青岛政府采购爬虫** 任务
2. 选择 **运行**

**方法 2：命令提示符**
```cmd
schtasks /run /tn "青岛政府采购爬虫"
```

### 检查推送

等待 2-5 分钟，查看钉钉群是否收到推送消息。

---

## 📊 第八步：查看和管理任务

### 查看任务运行历史

1. 打开 **任务计划程序**
2. 点击 **青岛政府采购爬虫** 任务
3. 查看底部 **运行历史记录** 标签

### 手动运行任务

```cmd
schtasks /run /tn "青岛政府采购爬虫"
```

### 查看任务状态

```cmd
schtasks /query /tn "青岛政府采购爬虫"
```

### 删除任务

```cmd
schtasks /delete /tn "青岛政府采购爬虫" /f
```

---

## ⚙️ 修改运行时间

### 方法 1：重新运行设置脚本

编辑 `setup_windows_task.bat` 文件，找到第 30 行：

```batch
schtasks /create /tn "青岛政府采购爬虫" /tr "...main.py..." /sc daily /st 09:00 /ru SYSTEM /f
```

修改 `/st 09:00` 为您想要的时间：
- `/st 08:30` → 早上 8:30
- `/st 18:00` → 下午 6:00
- `/st 22:00` → 晚上 10:00

然后重新以管理员身份运行脚本。

### 方法 2：使用任务计划程序图形界面

1. 打开 **任务计划程序**
2. 双击 **青岛政府采购爬虫**
3. 点击 **触发器** 标签
4. 点击 **编辑**
5. 修改时间为想要的时间
6. 点击 **确定** 保存

---

## 🐛 常见问题解决

### 问题 1：提示"未找到 Python"

**原因：** Python 未添加到 PATH 环境变量

**解决方法：**
1. 重新运行 Python 安装程序
2. 勾选 **Add Python to PATH**
3. 重新安装

或者手动添加：
1. 找到 Python 安装路径（如 `C:\Python311\`）
2. 右键 **此电脑** → **属性** → **高级系统设置**
3. 点击 **环境变量**
4. 在 **Path** 中添加 Python 路径
5. 重启命令提示符

### 问题 2：定时任务运行失败

**查看错误日志：**
1. 打开 **任务计划程序**
2. 双击任务
3. 查看 **运行历史记录**

**常见原因：**
- Python 路径错误
- 项目路径错误
- 权限不足

**解决方法：**
1. 删除旧任务：`schtasks /delete /tn "青岛政府采购爬虫" /f`
2. 重新运行设置脚本

### 问题 3：钉钉推送失败

**检查：**
1. 网络连接是否正常
2. 钉钉 Webhook 地址是否有效
3. .env 文件配置是否正确

### 问题 4：爬虫运行但找不到公告

**原因：** 网站结构可能已变化

**解决方法：**
1. 检查 `crawler.py` 中的网页选择器
2. 手动访问网站查看结构变化
3. 更新选择器配置

---

## 📋 快速命令参考

| 操作 | 命令 |
|------|------|
| 运行爬虫 | `python main.py` |
| 手动触发任务 | `schtasks /run /tn "青岛政府采购爬虫"` |
| 查看任务状态 | `schtasks /query /tn "青岛政府采购爬虫"` |
| 删除任务 | `schtasks /delete /tn "青岛政府采购爬虫" /f` |
| 查看任务历史 | 任务计划程序 → 运行历史记录 |

---

## 📞 需要帮助？

如果遇到问题，请提供以下信息：

1. **Windows 版本**：设置 → 系统 → 关于
2. **Python 版本**：`python --version`
3. **错误信息**：完整的错误提示
4. **任务历史**：任务计划程序中的运行历史记录

---

**设置完成日期：** 2026-04-05  
**适用系统：** Windows 10  
**默认运行时间：** 每天早上 9:00
